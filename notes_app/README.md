# Notes App

A Flask + SQLite notes app — create and delete simple text notes.

## Features

- Add a note (title + content)
- View all saved notes with timestamps
- Delete a note

## Built with

- Python, Flask, SQLite3

## Run locally

```
pip install flask
python app.py
```

Then open http://127.0.0.1:5000
